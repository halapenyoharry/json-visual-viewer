# Source — not needed in the VSIX
src/
src-vscode/
src-tauri/
_archive/
accessible-fonts/
accessible-fonts.zip
public/
research/

# Config / build tooling
vite.config.ts
vite.webview.config.ts
tsconfig*.json
eslint.config.js
eslint.config.*
.eslintignore
.gitignore
index.html
package-lock.json
.project-bible.json

# Personal / editor config — never ship
.claude/
.vscode/
.github/
.git/
.DS_Store
**/.DS_Store

# Dev artifacts
node_modules/
dist/
.vite/
*.log
HUD/
dist-extension/extension.js
dist-extension/extension.js.map
**/.$*

# Sample/demo data we don't need shipped (the user can drop in their own JSON)
dist-webview/projects-mass.json

# The extension only needs dist-extension/ and dist-webview/
